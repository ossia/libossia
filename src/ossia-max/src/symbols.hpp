#pragma once

#include "ext_proto.h"
namespace ossia::max_binding
{

static inline auto s_set = gensym("set");
static inline auto s_cell = gensym("cell");
static inline auto s_clear = gensym("clear");
static inline auto s_rows = gensym("rows");
static inline auto s_namespace = gensym("namespace");
static inline auto s_explore = gensym("explore");
static inline auto s_size = gensym("size");

}
