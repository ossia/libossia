//#include "later.h"
#include <cstdio>
#include <iostream>
// #include "../petrinet/conversion.h"
// #include "../../../../soft/dpetri/src/lib/petrinetserializer.h"

int main()
{
    /*
    Scenario s;
    TimeBox b1;
    b1.setStartEvent(s.getStartEvent());
    b1.setEndEvent(Event());
    TimeBox b2;
    b2.setStartEvent(b1.getEndEvent());
    b2.setEndEvent(s.getEndEvent());

    TimeBox b3;
    b3.setStartEvent(s.getStartEvent());
    b3.setEndEvent(s.getEndEvent());

    b1.getEndEvent().setDate(500);
    b2.getEndEvent().setDate(1000);

    s.addTimeBox(b1, b1.getStartEvent(), b1.getEndEvent());
    s.addTimeBox(b2, b2.getStartEvent(), b2.getEndEvent());
    s.addTimeBox(b3, b3.getStartEvent(), b3.getEndEvent());

    auto net = convertScenario(s);

    PetriNetSerializer<SimplePetriNet> serializer(net);
    auto txt = serializer.toSimpleText();
    std::cerr << txt;
*/

    //while(!later_test1)

    return 0;
}
